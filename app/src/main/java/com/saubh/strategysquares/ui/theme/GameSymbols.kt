package com.saubh.strategysquares.ui.theme

import androidx.compose.ui.graphics.Color

object GameSymbols {
    val defaultSymbols = listOf(
        "X" to Color(0xFF2196F3).value.toLong(), // Blue
        "O" to Color(0xFFF44336).value.toLong(), // Red
        "△" to Color(0xFF4CAF50).value.toLong(), // Green
        "□" to Color(0xFFFF9800).value.toLong(), // Orange
        "◇" to Color(0xFF9C27B0).value.toLong(), // Purple
        "○" to Color(0xFF795548).value.toLong(), // Brown
        "★" to Color(0xFF009688).value.toLong(), // Teal
        "♡" to Color(0xFFE91E63).value.toLong(), // Pink
        "♢" to Color(0xFF673AB7).value.toLong()  // Deep Purple
    )
}
